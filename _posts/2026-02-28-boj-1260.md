---
title: "[Baekjoon] 1260번: DFS와 BFS - C++ 풀이"
date: 2026-02-28 22:23:00 +0900
categories: [Baekjoon, cpp]
tags: [algorithm, graph, dfs, bfs]
math: true
---

## 문제 링크
[https://www.acmicpc.net/problem/1260](https://www.acmicpc.net/problem/1260)

## 문제 설명
그래프가 주어졌을 때, 정점 V에서 시작하여 DFS를 수행한 결과와 BFS를 수행한 결과를 출력한다.

- 정점 번호는 1부터 N까지이다.
- 같은 정점이 여러 개인 경우에는 정점 번호가 작은 것을 먼저 방문한다.

---

## 풀이 전략 (Approach)
이 문제는 그래프를 **DFS(깊이 우선 탐색)** 와 **BFS(너비 우선 탐색)** 로 각각 탐색하며 방문 순서를 출력하는 문제입니다.

### 1. 상태 정의 및 알고리즘 도출
`visited[i]`를 정점 i를 방문했는지 여부라고 정의하면,

- 인접 리스트 `adj`로 그래프를 저장한다.
- “번호가 작은 정점을 먼저 방문” 조건을 만족하기 위해 각 `adj[i]`를 오름차순 정렬한다.
- DFS는 재귀로 구현하여, 현재 정점에서 갈 수 있는 가장 작은 번호의 정점부터 깊게 방문한다.
- BFS는 큐로 구현하여, 현재 정점에서 갈 수 있는 정점들을 작은 번호부터 큐에 넣고 순서대로 방문한다.
- DFS 출력 후 `visited`를 초기화한 뒤 BFS를 수행한다.

---

## 코드 (C++)
```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>

using namespace std;

int n, m, v;
vector<vector<int>> adj;
vector<int> visited;

void init()
{
    cin >> n >> m >> v;

    adj.assign(n + 1, {});
    visited.assign(n + 1, 0);

    for (int i = 0; i < m; ++i)
    {
        int from, to;
        cin >> from >> to;
        adj[from].push_back(to);
        adj[to].push_back(from);
    }

    for (int i = 1; i <= n; i++)
    {
        sort(adj[i].begin(), adj[i].end());
    }
}

void dfs(int now)
{
    visited[now] = 1;
    cout << now << " ";

    for (int i = 0; i < (int)adj[now].size(); ++i)
    {
        int next = adj[now][i];

        if (visited[next] == 1)
            continue;

        dfs(next);
    }
}

void bfs()
{
    queue<int> q;
    q.push(v);
    visited[v] = 1;

    while (!q.empty())
    {
        int now = q.front();
        q.pop();

        cout << now << " ";

        for (int next : adj[now])
        {
            if (visited[next]) continue;
            visited[next] = 1;
            q.push(next);
        }
    }
}

void solve()
{
    init();

    dfs(v);
    cout << "\n";

    visited.assign(n + 1, 0);
    bfs();
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
    return 0;
}
```

---

## 복잡도 분석
### 1. 시간복잡도 : $$O((N + M) + \sum_{i=1}^{N} deg(i)\log deg(i))$$
그래프 저장 후, 각 정점의 인접 리스트를 정렬합니다.
정렬 비용은 각 정점 차수 deg(i)에 대해 $$deg(i)\log deg(i)$$ 이고 이를 모두 합한 값이 정렬 비용입니다.
DFS/BFS 탐색은 정점과 간선을 각각 최대 한 번씩 확인하므로 $$O(N+M)$$ 입니다.
따라서 전체 시간은 “정렬 + 탐색”으로 위와 같습니다.

### 2. 공간복잡도 : $$O(N + M)$$
인접 리스트에 간선을 저장하는 데 $$O(N+M)$$,
방문 배열에 $$O(N)$$ 공간이 필요합니다.
BFS 큐도 최악의 경우 $$O(N)$$까지 커질 수 있으므로 전체는 $$O(N+M)$$ 입니다.
